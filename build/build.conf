# Piler Version
# The Piler package itself (version, commit, checksums) is pinned in build/Dockerfile.
# Keep PILER_VERSION in sync with ARG PILER_VERSION in build/Dockerfile.

PILER_VERSION="1.4.9"

# MariaDB Version

MARIA_DB_VERSION="12.3.3"
